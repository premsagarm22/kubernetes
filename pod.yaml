# apiVersion:
# kind: 
# metadata:
# spec:

apiVersion: v1
kind: Pod
metadata:
  name: nginx-mirror
  annotations:
    kubernetes.io/config.mirror: "true"
spec:
  nodeName: prem
  containers:
  - name: nginx
    image: nginx:latest
    ports:
    - containerPort: 80
