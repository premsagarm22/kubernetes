# apiVersion: v1
# kind: Pod
# metadata:
#   name: nginx
# spec:
#   containers:
#   - name: nginx
#     image: nginx:latest
#     ports:
#     - containerPort: 80


apiVersion: v1
kind: Pod
metadata:
  name: nginx
  annotations:
    kubernetes.io/config.mirror: "true"
spec:
  containers:
  - name: nginx
    image: nginx
