# apiVersion:
# kind: 
# metadata:
# spec:

apiVersion: v1
kind: Pod
metadata:
  name: nginx
spec:
  nodeName: ip-172-31-49-196.ec2.internal
  containers:
  - name: nginx
    image: nginx:latest
    ports:
    - containerPort: 80
